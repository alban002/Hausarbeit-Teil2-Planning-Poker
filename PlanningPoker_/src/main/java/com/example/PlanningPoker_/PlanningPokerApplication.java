package com.example.PlanningPoker_;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PlanningPokerApplication {

    public static void main(String[] args) {
        // SpringApplication.run() wird verwendet, um die Spring Boot-Anwendung zu starten.
        // Es nimmt zwei Argumente: die Hauptklasse und die Kommandozeilenargumente (hier: args).
        SpringApplication.run(PlanningPokerApplication.class, args);
    }
}